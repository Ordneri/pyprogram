基于PySide6的自定义界面
(其实此时此刻UI已经改了很多了，但没有上传最新版，因为
最新版里面包含了毕业设计的关键代码，暂时不能公布，以后
会发布)
里面有不少优化空间，比如说用
self.setAttribute(Qt.WA_TranslucentBackground) + QPainter
可以直接实现自定义窗口，并且边角半透明，比这里的setMask性能高，效果好
再比如corner_tip_list,需要在构造函数最下面加一个resize(0,0)
mainw主窗口里面会有一个幽灵窗口

后面还会添加不少有用的功能，（实际上此时此刻的功能已经完善不少了）
但一时半会不会发布，等毕设交了再说吧